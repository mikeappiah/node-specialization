<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Products</title>
	</head>
	<body>
		<ul>
			<% if (products.length > 0) { %> 
                <% for (let product of products) { %>

			<li>
				<b><%=product.category %></b> - <%=product.name %> - <%= product.price
				%>
			</li>

			<% } %> 
            <% } %> <% else { %>
			<h1>No product found</h1>
			<% } %>
		</ul>
	</body>
</html>
